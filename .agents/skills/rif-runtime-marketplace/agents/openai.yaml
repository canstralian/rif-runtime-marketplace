interface:
  display_name: "Rif Runtime Marketplace"
  short_description: "Repo-specific patterns and workflows for rif-runtime-marketplace"
  default_prompt: "Use the rif-runtime-marketplace repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true